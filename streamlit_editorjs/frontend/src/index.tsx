import React from "react"
import ReactDOM from "react-dom"
import EditorJSDIV from "./editorjs"

ReactDOM.render(
  <React.StrictMode>
    <EditorJSDIV />
  </React.StrictMode>,
  document.getElementById("root")
)
